import { ColorValue } from 'react-native';

const LinearColors: {
  PurpleLinear: readonly [ColorValue, ColorValue];
  BlueLinear: readonly [ColorValue, ColorValue];
} = {
  PurpleLinear: ['#C58BF2', '#EEA4CE'],
  BlueLinear: ['#92A3FD', '#9DCEFF'],
};

export default LinearColors;
